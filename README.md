# Markdown-link-checker
Scans Markdown folders to instantly find broken local file links. Unlike heavy plugins, it’s a lightweight standalone script that skips external links, anchors, and system junk. Quick Start: pip install pydantic &amp; run python main.py.
